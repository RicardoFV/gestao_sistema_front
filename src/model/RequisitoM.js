// criando a classe
export default class RequisitoM {

    constructor(id, sessao, nome, descricao, tipo_requisito) {
        this.id = id
        this.sessao = sessao
        this.nome = nome
        this.descricao = descricao
        this.tipo_requisito = tipo_requisito
    }
}