// criando a classe
export default class RequisitoM {

    constructor(sessao, nome, descricao, tipo_requisito) {
        this.sessao = sessao
        this.nome = nome
        this.descricao = descricao
        this.tipo_requisito = tipo_requisito
    }
}