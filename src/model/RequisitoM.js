// criando a classe
export default class RequisitoM {

    constructor(id, sessao, name, descricao, tipo_requisito) {
        this.id = id
        this.sessao = sessao
        this.name = name
        this.descricao = descricao
        this.tipo_requisito = tipo_requisito
    }
}