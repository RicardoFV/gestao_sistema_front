// criando a classe
export default class AutenticacaoM {

    constructor(id, email, senha, sessao) {
        this.id = id
        this.email = email
        this.senha = senha
        this.sessao = sessao
    }
}